package com.techmart.config;

/**
 * JMS destinations are configured on the application server.
 * GlassFish: created via asadmin (jms/queue/OrderQueue, jms/topic/InventoryTopic).
 * WildFly: configured in standalone-full.xml or via @JMSDestinationDefinitions.
 */
public class JMSConfig {
}
